FOUNDER — CONSOLIDATED INTEGRATION ORDER: every feature discussed since the Chain of Proof investigation, mapped into the ATLAS phases. Nothing here reorders the standing sequence (hotfix → P0 → foundation → intelligence → action → ecosystem → harden → validate → deliver); it assigns homes. Persist as /app/docs/ATLAS-FEATURE-MAP.md and keep it current as items land.

P0 TRUTH / PROOF FEATURES: outcome rows on /standards (median days-to-first-response, interviews per 100 apps, honest n, never-fabricate); public claim-verification ladder (owner-attested → document-backed → third-party-checked, defined per tier); operator page; readable privacy policy; brand sweep; live version string; /standards synced + change history; robots/sitemap; Time-to-Qualified-Interview instrumentation with guardrails.
FOUNDATION (P3 ATLAS): licensed partner-feed connectors on the SDK (aggregator publisher APIs — lawful "millions", attribution honored, source-policy gated); registry/policy/SDK/graph/freshness≠liveness/dedup/security as queued.
INTELLIGENCE (P4): exclusion reason codes visible on the feed (no_work_auth, cap_reached, stale_posting…); salary-floor honesty signal (flag postings omitting pay vs the candidate's floor rather than silently passing); eligibility write-back — with candidate authorization, pre-answer employer work-auth/sponsorship screeners from the public-data engine; Fastest Path + lanes as queued.
ACTION (P5): UNLIMITED READINESS / HUMAN SEND — every eligible job in the spectrum gets a complete grounded packet instantly with its official destination; the cap applies to sends only, never to preparation; route engine as queued. Own-mailbox outreach — consent-scoped OAuth Gmail/Outlook send for follow-ups and recruiter notes, provider-rate-limited, human-approved per send, reputation on the candidate's account not the platform. One-tap withdraw with receipt. Reviewed receipts (employer confirms a candidate was read → receipt to candidate). One-tap structured rejection reasons (five options) feeding rejection autopsy. Passport share to immigration counsel (consent-scoped view, same share-link machinery). Weekly receipts email (config-flagged). Interview outcome receipts.
ECOSYSTEM (P6): consented employer network / Green Lane with response-SLA pledge ranked in the feed; employer pool page (anonymized counts by role family/region/eligibility — the answer to "n?"); responds-fast badge (no negative badges); warm-intro engine (network layer per ATLAS §26); partner API; employer dashboard read-only v1.
CEILING RUNGS (spec + foundations only unless a lawful dependency exists; label horizon honestly in ATLAS-STATE): R1 Passport as standing availability signal (consent-scoped outward Passport-as-API — build the scope + query surface; no employer browsing); R2 demand-as-data (structured requirement publishing in the employer system — build schema + intake; gates run in reverse); R3 agent-to-agent negotiation (write the open protocol spec + a reference endpoint; no autonomous irreversible action); R4 portable outcome reputation (signed cross-platform export format from existing receipts); R5 evidence-read skills + demand routing ("one credential away" lane wired to verified-demand counts; credential partners = CONFIGURATION_REQUIRED); R6 income continuity / bridge lane (marketplace partner connectors on the SDK; honest empty until a partner exists); R7 employers compete on response in public (published per-employer response medians once n is honest); R8 global mobility path (country packs + eligibility engine; counsel-flagged).
EXPLICITLY NOT INTEGRATED (constitutional): universal scraping, unlimited sends/cold email from the platform domain, CAPTCHA/auth/robots bypass, form-route auto-submit before the measured >99% accuracy lock, data sale, pay-to-rank.
Gate each phase; merge on PASS; publish clicks remain the founder's; hotfix ships first; checkpoint on interruption.
