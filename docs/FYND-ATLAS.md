FOUNDER — FYND ATLAS SUPERSEDES the Planetary Constitution as the standing program (same order: hotfix → P0 truth audit → foundation → intelligence → action → ecosystem → harden → validate → deliver). Persist as /app/docs/FYND-ATLAS.md. Everything already queued stands; these are the ADDITIONS and SHARPENINGS:

ADD — NETWORK & REFERRAL LAYER: opt-in candidate-imported contacts, alumni networks, communities, employer ambassadors, referral requests, warm-intro paths. Rules: never scrape private graphs, never message without approval, never impersonate, never expose a contact graph, never infer private relationships, imported contacts revocable, every outbound message previewable.
ADD — OPPORTUNITY TRUST SCORE (anti-scam): observable signals only (verified domain/org, official ATS, gov registration, source history, contact-domain consistency, comp plausibility, description duplication, suspicious redirects, payment/identity requests, off-platform pressure, candidate reports, removal history); actions warn → require verification → suppress → quarantine → block → human review → notify affected candidates; never assert an employer is safe; scam reporting + appeal paths.
ADD — PARTNER API FOUNDATION: partner API, signed webhooks, feed ingestion, opportunity corrections, closure updates, outcome callbacks, consent-aware candidate exchange, sandbox, API keys, usage limits, audit logs, versioning + deprecation policy; no candidate data without required authorization.
ADD — JOURNEY STAGES Compare / Interview / Offer / Learn: offer comparison with normalized comp, benefits, cost of living, location, candidate priorities (no tax/immigration/legal guarantees); interview briefs from approved evidence; negotiation prep; outcome learning that may never harden into discriminatory ranking.
ADD — BUSINESS-MODEL RULES: permitted candidate/employer subscriptions, premium prep, verified-employer services, partner feeds, institutional partnerships; forbidden data sale, pay-to-rank, hidden sponsored jobs, charging for privacy rights, volume-based incentives, employer access to sealed data, predatory outcome fees. Cost model per §36.
ADD — DESIGN STANDARD: eleven visual states (verified, candidate-approved, inferred, unknown, blocked, stale, closed, restricted, handoff required, action required, completed) as a design-system token set; motion communicates state or causality only; no endless feeds, fake urgency, opaque AI scores, decorative maps, excessive glass.
SHARPEN — Source policy: robotsStatus, termsStatus, licenseStatus, legalReviewStatus as four separate statuses; operations {discover, fetch, monitor, paginate, interact, authenticate, prepare, submit}. Six planes (Candidate truth split from Candidate action). Connector SDK adds normalize(). Claim model per §19 (category, value, source, evidence, status, confidence, approval, version, sensitivity, sharing rules, effective dates). Employer trust system per §27 incl. verified domain, listing correction, removal requests, closed-role marking. Ops console per §38 (every chart supports a decision). Fairness: correction and appeal paths.
ZERO-TOLERANCE LAUNCH BLOCKERS (§44) — encode as CI/gate checks where testable: unauthorized external action, fabricated claim, protected-trait ranking, data/secret exposure, duplicate application, restricted-platform circumvention, prompt-injection control failure, silent unknown→pass/active, sample data in production metrics, unsupported coverage claim, missing policy enforcement, missing kill switch.
CONTINUITY (§47): maintain /app/docs/ATLAS-STATE.md (mission, invariants, decisions, policies, schemas, supported sources, country coverage, completed, remaining, test results, known failures, blockers, deploy state, next action). On any forced stop leave the repo runnable and write ONE exact continuation instruction.
FINAL RESPONSE CONTRACT per §49; never claim universal coverage, legal compliance without review, production readiness without gates, automated submission where only handoff exists, or verified liveness where unknown. Rails unchanged and absolute.
