FOUNDER — PLANETARY OPPORTUNITY INTELLIGENCE: the standing program that follows the hotfix. On credit restoration execute in STRICT order; no-questions policy, decide-and-document, precision protocol, gate per phase, merge pre-authorized on PASS, publish clicks remain the founder's. Persist this message verbatim as /app/docs/PLANETARY-CONSTITUTION.md before any work.

ORDER OF OPERATIONS
P-HOTFIX (first, unchanged): the queued onboarding trio — parse pipeline root cause, manual-claim entry mounted (identity + education/employment → draft → approve → activation reachable with zero uploads), Finish-Passport CTA fixed, CI rule that inert primary CTAs fail. Report HOTFIX READY FOR RE-PUBLISH.
P0 TRUTH AUDIT (public claims vs implementation; fix critical trust gaps): (a) /privacy-policy serves a legacy OpportunityOS shell with no text — serve the real policy as static crawlable text, versioned; (b) /employers contact address is on opportunityos.dev — brand sweep of every served route/cache; (c) footer "v0.1" stale — version string from build; (d) /standards lists Phases 0–4 only — sync gates to actual merged phases and add a change history; (e) publish an operator/about page (legal entity, contact); (f) scaffold outcome rows on /standards (median days-to-first-response, interviews per 100 apps) with the existing never-fabricate rule — render "measuring" until n is honest; (g) robots.txt + sitemap.xml real files; (h) instrument the north-star: Time to Qualified Interview + the guardrail metrics (application→response, response→interview, false-pass/false-exclusion, duplicate rate, closed-listing exposure, consent-violation count) — measure baselines before any optimization.
P1 CORE GLOBAL ARCHITECTURE (five planes: Acquisition / Intelligence / Candidate action / Trust+governance / Experience — clear interfaces, no premature microservices):
 1. SOURCE ACCESS POLICY ENGINE — machine-readable SourceAccessPolicy per source {sourceId, jurisdiction[], allowedOperations{discover,read,monitor,paginate,interact,authenticate,prepareApplication,submitApplication}, accessMethods[official_api|ats_api|partner_feed|open_data|public_page|search|candidate_import|candidate_handoff], robots/terms/license reviewedAt, legalReviewStatus approved|conditional|pending|prohibited, requiresCandidateSession/Approval/HumanAttestation, rateLimit, retentionDays, attributionRequired, removalProcedure, policyVersion, effectiveAt, killSwitch}. Every external worker action asks the engine first (enabled? operation permitted? policy current? consent present? human approval? rate limit? quarantine? kill switch?). Denial fails closed with an auditable reason. Deterministic — no LLM in the policy path.
 2. GLOBAL SOURCE REGISTRY — the existing 16 verified providers migrate in as the first records; full lifecycle Discovered→Policy review→Connector dev→Test→Shadow ingestion→Quality review→Limited release→Active→Monitored→Quarantined/Retired; no source skips shadow.
 3. CONNECTOR SDK — interface OpportunitySourceConnector {source(), discover?(), fetch(), parse(), checkLiveness(), resolveApplicationRoute(), monitor?(), healthcheck()}; every connector ships fixtures, schema validation, parser/liveness/duplicate tests, rate-limit + retry + failure classification, policy declaration, attribution, removal, monitoring. Refactor existing Greenhouse/Lever/Ashby ingestion onto the SDK — no giant conditional scraper.
 4. CANONICAL OPPORTUNITY MODEL — shared core (ids, org + verification state, source, original/canonical URL, geo, remote scope + eligible remote countries, original + display language, original + translated description preserved verbatim, lifecycle, dates, freshness, liveness, compensation normalized with original kept, eligibility, skills, credentials, route, extraction confidence, provenance, content hash, duplicate cluster, policy status) + category extensions (employment, internship, apprenticeship, fellowship, freelance, scholarship, grant, competition, training, mentorship). Jobs remain primary; extensions ship as schema + fixtures, populated only when a lawful source exists.
 5. FRESHNESS ≠ LIVENESS — separate fields and budgets; liveness states active|inactive|unknown|temporarily_unreachable|requires_candidate_verification; unknown never silently becomes active; strongest allowed liveness check before any prepare/approve.
 6. ENTITY RESOLUTION + DEDUP — clusters, never deletion; canonical-source priority official API > ATS record > employer page > government > licensed > directory > search copy; never submit the same candidate to the same requisition twice (already enforced — keep).
 7. HOSTILE-CONTENT DEFENSE — protocol allowlist, private/link-local/metadata-IP blocking with safe DNS re-resolution, redirect + size + timeout limits, MIME validation, HTML sanitization, script stripping, structured-output validation, retrieved text isolated from privileged prompts (a webpage can never change our rules), signed webhooks, policy decisions logged, unnecessary PII redacted. Tests for SSRF, private IP, cloud metadata, dangerous protocols, redirect abuse, prompt injection, oversized content, webhook forgery.
 8. MATCHING CONSTITUTION — Stage 1 deterministic qualification gates (pass|fail|unknown|candidate_confirmation_required: liveness, deadline, work auth, sponsorship, citizenship/residency, ITAR, clearance, credential, education, experience, language, location, remote scope, schedule, comp floor, exclusions, duplicate, employer cap, platform policy) strictly separate from Stage 2 explainable ranking; every result shows why passed, why ranked, positive/negative/unknown factors, evidence, and what would change it; no protected attributes, no inference of them, no auto-penalty for gaps, no false precision.
 9. APPLICATION ROUTE ENGINE — every opportunity resolves to official_api_submit|approved_ats_submit|public_form_assisted|email_assisted|external_authenticated_handoff|candidate_managed|unsupported, displaying route, automation level, required candidate actions, limitations, approval requirement, authorization expiry, expected receipt. Permitted submission: validate materials+consent+liveness+duplicate+caps+route policy → lock material hash → require approval → submit → receipt. Restricted: prepare packet → open official destination → show remaining steps → candidate confirms result → candidate-confirmed record.
 10. FASTEST PATH ENGINE — home screen answers "single best next action" (approve a claim, correct eligibility, review a fit, complete a screener, approve an application, follow up, prepare interview, verify deadline…) ranked by expected value, urgency, time, liveness, confidence, reversibility, risk; no fabricated probabilities; every action explained.
 11. LANES — Best Fit, Fastest Credible Response (only with sufficient n, privacy-safe, confidence shown, never a guarantee), Local Now, Remote Worldwide, Visa-Friendly, Sponsorship Possible, New Today, Closing Soon, Government, Internships, Apprenticeships, Outside My Usual Path, One Credential Away — each lane exposes its logic. Income Now ships only if a lawful source exists; otherwise honest empty.
 12. I18N FOUNDATION — locale suggestion with permission before precise location, manual country/region, search outside current country, currency/date/timezone/salary-period localization, RTL-ready, low-bandwidth mode, installable PWA. Country packs are a framework + readiness gates (Research→Policy→Source build→Shadow→Native QA→Pilot→Limited→GA); no country is "supported" because its language is in a selector.
 13. AI BOUNDARIES — LLMs only for extraction, classification, translation, normalization, summarization, grounded materials, prep, explanations, anomaly detection; deterministic code for consent, authorization, policy, rate limits, hard gates, dedup, caps, state transitions, hashing, receipts, retention, billing, deletion, security. Every consequential AI result carries evidence, confidence, model version, prompt/ruleset version, schema + grounding validation, correction path, failure state. No LLM performs an irreversible external action.
P2 PILOT MARKETS — select by RESEARCH, not assumption: candidate demand, source availability (official APIs/open data first), language readiness, ATS coverage, employer density, legal feasibility, cost, opportunity diversity. Write the selection memo with sources before building. Do not assume the US or India.
P3 REQUIRED VERTICAL SLICE (the completion bar): one real lawful source → policy-authorized acquisition → raw snapshot → extraction → validation → normalization → provenance → freshness → liveness → org resolution → dedup → index → qualification → explainable ranking → candidate review → grounded prep → explicit approval → supported submission or handoff → receipt → tracker → outcome; plus registry, policy engine, connector interface, source health, kill switch, retries, dead-letter, admin review, provenance visible in the candidate UI, tests, docs, deploy config. Do not fake integrations: unavailable platforms get contract + fixtures + policy state, labelled accurately.
P4 EMPLOYER TRUST NETWORK — org ownership verification (domain), ATS connect, structured opportunity publish, eligibility declaration, close-role, authorized packet receipt, clarification requests, outcome recording, source-removal requests, signal-quality monitoring; candidates protected from browsing, bulk export, hidden profiling, protected-attribute filtering, unbounded retention, unapproved contact, resale.
P5 OPPORTUNITY EXPANSION beyond jobs — only after the job journey meets quality thresholds.
CROSS-CUTTING: evaluation science (golden datasets per stage; precision/recall/F1, cluster purity, active-listing precision, closed-listing recall, false pass/exclusion, unsupported-material rate, explanation faithfulness; offline→shadow→canary→human review; no model enters production silently). Privacy/fairness/compliance artifacts (data inventory, purpose map, consent map, data-flow map, retention schedule, subprocessor register, RoPA outline, DPIA, threat model, rights workflow, takedown process, incident plan; flag counsel-required items). Security architecture per the threat model (sessions, hashing, MFA-ready, least privilege, row-level authz, encryption, secrets, webhook verification, URL safety, sanitization, audit logs, rate limits, abuse detection, employer verification, scam reporting, dependency scanning, safe deletion). Performance targets measured against baselines first (feed p95 <500ms, detail p95 <300ms, approval validation p95 <2s, priority ingestion→match p95 <5min, 99.9% read availability), with logs/metrics/traces/dashboards/alerts/runbooks/canary/rollback. Self-improving coverage loop that RECOMMENDS connectors and never self-approves prohibited or legally uncertain access; candidate-facing coverage honesty labels strong|moderate|limited|discovery_only|handoff_only|unsupported; never market incomplete coverage as universal.
DELIVERABLES per §36 as /app/docs/planetary/*: audit, PRD, ADRs, architecture + data-flow, graph, registry, policy engine, SDK, schemas, country-pack framework, ingestion/dedup/freshness/liveness/search/matching/ranking/action/route/employer/ops docs, privacy + threat + fairness, tests + golden datasets, monitoring, runbooks, deployment + migration guides, cost model, rollout plan, coverage map, supported + restricted source lists, risk register, decision log, backlog, README.
FINAL REPORT CONTRACT per §38: what existed, what was discovered, what changed, what works end to end, which sources/countries are supported, which require handoff, which integrations are pending, what was tested, where deliverables are, largest product/technical/compliance risks, highest-value next expansion. Never claim universal coverage; never say production-ready unless §37 gates pass.
RAILS ABSOLUTE: no auth/CAPTCHA/robots/rate-limit bypass; no credential collection; no scraping of prohibited platforms; no unauthorized auto-submit; no data sale; no unlabelled sponsored ranking; no irreversible LLM action; consent revocation stops processing; samples/fixtures never in production metrics; .env tripwire on every push. Checkpoint-commit on any interruption.
